# os module is something similar to the terminal
# it allows you to read write or edit files 
# also we can run the operating commands here
# in simple words os module in python allows us to interacting with operating system

import os
# print(os.name)   # this will print the name of operating system

# xyz = open("jerry.txt", "r")
# contents = os.read(xyz, os.fstat(xyz).st_size)
# os.close(f)
# print(xyz)



# kuch toh glitch hai , run nahi ho raha os module, terminal main khelo iske saath.
# i think ki ye itna kuch zarurui bhi nahi hai , os module ka saara kaam terminal pe ho jayega. (not sure about this)