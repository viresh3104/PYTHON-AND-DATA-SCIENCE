# n = int(input("Enter the number of scores : "))

# input_scores = list(map(int, input("Entered scores are seperated by spaces :").split()))

# maximun_score_in_input_scores = max(input_scores)

# runner_up_score = (input_scores for input_scores in input_scores != maximun_score_in_input_scores)

# max_in_runner_up = max(runner_up_score)
# print(max_in_runner_up)