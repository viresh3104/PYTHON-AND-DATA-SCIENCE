# Array Initialization: Write a function to create a 2D array of size m×n with all elements initialized to zero
import numpy as np
def zero_array():
    arr = np.zeros((2,2))
    print(arr)

zero_array()